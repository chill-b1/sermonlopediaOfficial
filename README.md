# Wiki.js Docker Application Setup

This repository contains the necessary files to build and run the Wiki.js application inside a Docker container.

## **In order for this to work, you must create a secrets/ directory and add a password.txt file to that directory
1. Inside that file should only contain the plain english database password, then this will all be able to run.
